- if keywords
  .hori-cate-area
    .hot-keywords
      .wrapper
        - keywords.each do |keyword|
          %a{href: "/zhekou/#{URI.encode_www_form_component(keyword)}/", title: "#{keyword}折扣"} #{keyword}

- home_anchor = ['内部券','内部优惠券','优惠券网站','天猫内部优惠券','淘宝内部券'].sample
%p.bread-area
  %span 内部优惠券：
  %a{href: "/", title: "#{home_anchor}}"} #{home_anchor}
  - if cate
    %span &nbsp;&nbsp;#{">"}&nbsp;&nbsp;
    %a{href: "/zhekou/#{URI.encode(cate)}/", title: "#{cate}折扣"} #{cate}
  - if sub_cate
    %span &nbsp;&nbsp;#{">"}&nbsp;&nbsp;
    %a{href: "/zhekou/#{URI.encode(sub_cate)}/", title: "#{sub_cate}折扣"} #{sub_cate}
  %span &nbsp;&nbsp;#{">"}&nbsp;&nbsp;
  %span #{title}


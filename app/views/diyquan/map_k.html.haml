- title(@title)
- head_desc(@description)
- keywords(@keywords)
- path(@path)
- mobile_url(@path)
- h1(@h1)

= render partial: "diyquan/header_fenlei_zhekou"
= render partial: "diyquan/suggest_keywords", locals: {keywords: @ks.map{|k| k.keyword}}
.hori-cate-area
  .hot-keywords
    .wrapper
      - @pages.each do |page|
        %a{href: "/map_k/#{page}/"} #{page}

= render partial: "diyquan/footer"
